;===[ Начало сегмента кода ]=========================================================================
MYCODE: segment .code
org 100h; Обязательная директива для COM-файлов
START:;---[ Точка старта ]---------------------------------------------------------------------------



    call no32
    ;call no33
    ;call no34
    ;call no35
    ;call no36
    ;call no37
    ;call no38
    ;call no39
    ;call no40
    ;call no41
    ;call no42



;---[ Стандартное завершение программы ]-------------------------------------------------------------
    mov AX, 4C00h
    int 21h




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no32:
    align  16, db '.'
    db '[=MYDATA BEGIN=]'
    db 'A1A2A3 = ['
    A1 db 12h
    A2 db 34h
    A3 db 56h
    db ']'
    align  16, db '.'
    db 'B1B2 = ['
    B1 db 56h
    B2 db 78h
    db ']'
    align  16, db '.'
    db 'C1C2C3 = ['
    C1 db 00h
    C2 db 00h
    C3 db 00h
    db ']'
    align  16, db '.'
    times 16 db '.'
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------



;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no33:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BL, byte[B1]
    add AL, BL
    jnc m1
    inc AH
    m1:

    mov byte[C1], AH
    mov byte[C2], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no34:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BL, byte[B1]
    sub AL, BL
    jnc m3
    dec AH
    m3:

    mov byte[C1], AH
    mov byte[C2], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no35:
    mov AH, byte[A1]
    mov AL, byte[A2]
    SHL AX, 2
    
    mov byte[C1], AH
    mov byte[C2], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no36:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov DL, byte[A3]
    mov BL, byte[B1]
    add BL, AL
    jnc m11
    inc AL
    jnc m11
    inc AH
    m11:

    mov byte[C1], AH
    mov byte[C2], AL
    mov byte[C3], DL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no37:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov DL, byte[A3]
    mov BL, byte[B1]
    sub AL, DL
    jnc m111
    dec AL
    jnc m111
    dec AH
    m111:

    mov byte[C1], AH
    mov byte[C2], AL
    mov byte[C3], DL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no38:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BH, byte[B1]
    mov BL, byte[B2]
    add AL, BL
    jnc m22
    inc AH
    m22:
    add AH, BH
    mov byte[C1], AH
    mov byte[C2], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no39:
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BH, byte[B1]
    mov BL, byte[B2]
    sub AL, BL
    jnc m222
    dec AH
    m222:
    sub AH, BH
    mov byte[C1], AH
    mov byte[C2], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no40:
    mov CH, byte[B1]
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BH, byte[A3]
    mov BL, byte[A4]

    add BL, CH
    jnc m2222
    inc BH
    m2222:
    jnc m22222
    inc AL
    m22222:
    inc AH

    mov byte[C1], AH
    mov byte[C2], AL
    mov byte[C3], BH
    mov byte[C4], BL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no41:
    mov CH, byte[B1]
    mov AH, byte[A1]
    mov AL, byte[A2]
    mov BH, byte[A3]
    mov BL, byte[A4]

    sub BL, CH
    jnc m30
    dec BH
    m30:
    jnc m31
    dec AL
    m31:
    dec AH

    mov byte[C1], AH
    mov byte[C2], AL
    mov byte[C3], BH
    mov byte[C4], BL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------




;---[ Начало подпрограммы ]--------------------------------------------------------------------------
no42:
    mov CL, byte[B1]
    mov AH, byte[A1]
    mov AL, byte[A2]

    mov CH, 0
    mul CX
     
    mov byte[C1], DL
    mov byte[C2], AH
    mov byte[C3], AL
ret
;---[ Конец подпрограммы ]---------------------------------------------------------------------------
