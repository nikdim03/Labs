1. Дана строка символов, нужно ее ввести, используя функцию calloc(). Память необходимо захватывать под некоторое фиксированное значение элементов (2-5). Если памяти не хватает, то добавить её, используя функцию realloc().

2. Дана строка символов, нужно ввести её, используя функцию malloc(). При вводе очередного символа необходимо захватить память с помощью функции realloc().

3. Ввод строки с клавиатуры, помощение её в файл, считываем данные из файла, производим её обработку и результат помещаем в другой файл.